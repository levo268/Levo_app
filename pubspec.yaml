name: levo_app
description: Learn Together. Level Together.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
  assets:
    - assets/logo.png
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  google_fonts: ^6.2.1
  font_awesome_flutter: ^10.7.0
  assets:
    - assets/logo.png

flutter:
  assets:
    - assets/logo.png
  uses-material-design: true
